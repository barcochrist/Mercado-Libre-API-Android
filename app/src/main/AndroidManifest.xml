<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.programacionmaster.mercadolibreapijava">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResourcesWithAuthActivity"
            android:label="@string/title_activity_resources_with_auth" />
        <activity
            android:name=".ResourcesWithoutAuthActivity"
            android:label="@string/title_activity_resources_without_auth" />
        <activity
            android:name=".ResourceDetailActivity"
            android:label="@string/title_activity_resource_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ResourceResultActivity" />
    </application>

</manifest>